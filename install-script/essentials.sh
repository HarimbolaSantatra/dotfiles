#!/usr/bin/env bash

# auto-tools
sudo apt-get install -y autotools-dev
sudo apt-get install -y automake

# Git
sudo apt-get install -y git

# GnuTLS: tools to generate certificates for the verification of host identity and the encryption of client/server communications. 
# in order to clone a github repo
sudo apt install -y gnutls-bin

# install tmux
sudo apt-get install -y tmux
